{
  "name": "metahousesearcher-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:analyze": "node build.js --analyze",
    "build:from-seed": "node build.js --from-seed",
    "seed": "node build.js --seed-only",
    "deploy": "node build.js && git add docs && git commit -m \"Update search results\" && git push"
  },
  "dependencies": {
    "puppeteer": "^25.1.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "@xenova/transformers": "^2.17.2"
  }
}
